print(*(lambda nums: ((lambda gmep, nums: [gmep.__setitem__(0, gmep[0] * 2 + (sum(n[i] == '1' for n in nums) > len(nums) / 2)) or gmep.__setitem__(1, gmep[1] * 2 + (sum(n[i] == '1' for n in nums) <= len(nums) / 2)) for i in range(len(nums[0]))] and gmep[0] * gmep[1])([0, 0], nums), (lambda lst: [lst.__setitem__(0, [n for n in lst[0] if n[i] == ('1' if sum(n[i] == '1' for n in lst[0]) >= len(lst[0]) / 2 else '0')] or lst[1]) for i in range(len(lst[0][0]))] and [lst.__setitem__(1, [n for n in lst[1] if n[i] == ('1' if sum(n[i] == '1' for n in lst[1]) < len(lst[1]) / 2 else '0')] or lst[1]) for i in range(len(lst[1][0]))] and (int(lst[0][0], 2) * int(lst[1][0], 2)))([nums.copy(), nums.copy()])))(__import__('sys').stdin.read().split()), sep='\n')